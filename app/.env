# Base URL for the map tiles (PMTiles). Points at the Cloudflare R2 public bucket
# so the app streams tiles via HTTP range requests instead of shipping them in git.
# Leave empty to serve tiles from the app's own origin (app/public/tiles/).
VITE_TILES_BASE_URL=https://pub-1b16e6dbfbeb4116b4cc74d50cc39952.r2.dev
