# Base URL for municipality PMTiles (public read-only Cloudflare R2 bucket).
# Leave empty to serve tiles from the app's own origin (app/public/tiles/).
# Omit this variable entirely to use the hosted public bucket in production builds.
VITE_TILES_BASE_URL=https://pub-1b16e6dbfbeb4116b4cc74d50cc39952.r2.dev
